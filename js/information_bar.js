let initInform = (username, date)=>{
    let result = `
    <h3 class="logo">To Do List</h3>
    <p class="date">${date}</p>
    <div class="user"><p class="username">${username}</p></div>
    `

    return result;
}
